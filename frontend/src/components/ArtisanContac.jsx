import React from "react";
import { Link } from "react-router-dom";

function ArtisanContact({ artisan }){

    return(
        <div className="ArtisanContact">
            <form action={}>

            </form>
        </div>
    );
}